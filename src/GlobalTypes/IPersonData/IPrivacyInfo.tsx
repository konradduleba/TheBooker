export default interface IPrivacyInfo {
    seeFuturePosts: string;
    following: string;
    friendRequest: string;
    friendList: string;
    tag: string;
    phoneNumber: string;
    emailAddress: string;
}